export const privateRoutes = ["/bashID", "/dashboard", "/settings", "/landing"];

export const authRoutes = [
  "/auth/login",
  "/auth/register",
  "/auth/error",
  "/auth/new-verification",
];
export const apiAuthPrefix = "/api/auth";
export const DEFAULT_AUTH_REDIRECT = "/landing";
