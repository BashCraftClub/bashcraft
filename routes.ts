export const publicRoutes = ["/", "/auth/new-verification", "/landing"];

export const authRoutes = ["/auth/login", "/auth/register", "/auth/error"];
export const apiAuthPrefix = "/api/auth";
export const DEFAULT_AUTH_REDIRECT = "/landing";
